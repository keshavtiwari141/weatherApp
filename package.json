{
  "name": "webserver",
  "version": "1.0.0",
  "description": "const path = path.join(__dirname, '../public') express.set('view engine', path) for add header in express (hbs.partialRegister(path of partials)) partial add in html {{>header}} any value add in handelbar {{}}",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "hbs": "^4.1.1",
    "request": "^2.88.2"
  }
}
